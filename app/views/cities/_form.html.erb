<% form_for @city, :html => {:id => "cadastro"} do |f| %>
  <%= f.error_messages %>
  <p>
    <label>Estado</label>
    <%= f.collection_select :state, City::STATES, :to_s, :to_s %>
  </p>
  <p>
    <label>Nome</label>
    <%= f.text_field :name %>
  </p>
  <p>
    <label>Código</label>
    <%= f.text_field :code %>
  </p>
  <p>
    <%= link_to image_tag("voltar.png", :border => 0), cities_path %>
    <%= image_submit_tag 'cadastrar.png', :id => "bt_form" %> 
  </p>
<% end %>
