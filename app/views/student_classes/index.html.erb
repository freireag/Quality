<h1>Listando turmas de <%= @student.name %></h1>

<table>
  <tr>
    <th>Curso</th>
    <th>Turma</th>
  </tr>

<% @student_classes.each do |student_class| %>
  <tr>
    <td><%=h student_class.course_class.course.name %></td>
    <td><%=h student_class.course_class.code %></td>
    <td><%= link_to 'Destroy', student_class, :confirm => 'Você tem certeza?', :method => :delete, :student_id => @student.id %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Adicionar Turmas', new_student_student_class_path(@student) %>

